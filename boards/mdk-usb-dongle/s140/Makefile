include ../../Makefile.boards

CFLAGS += -DBOARD_CUSTOM
CFLAGS += -UCONFIG_GPIO_AS_PINRESET

ASMFLAGS += -DBOARD_CUSTOM
ASMFLAGS +=-UCONFIG_GPIO_AS_PINRESET

ifeq ($(PBRICK_DEBUG), 1)
CFLAGS += -DNRF_LOG_BACKEND_UART_TX_PIN=8
ASMFLAGS += -DNRF_LOG_BACKEND_UART_TX_PIN=8
endif