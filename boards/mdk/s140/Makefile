include ../../Makefile.boards

CFLAGS += -DBOARD_CUSTOM
CFLAGS += -DCONFIG_GPIO_AS_PINRESET

ASMFLAGS += -DBOARD_CUSTOM
ASMFLAGS += -DCONFIG_GPIO_AS_PINRESET

ifeq ($(DEBUG), 1)
CFLAGS += -DNRF_LOG_BACKEND_UART_TX_PIN=20
CFLAGS += -DNRF_LOG_BACKEND_UART_RX_PIN=19
ASMFLAGS += -DNRF_LOG_BACKEND_UART_TX_PIN=20
ASMFLAGS += -DNRF_LOG_BACKEND_UART_RX_PIN=19
endif