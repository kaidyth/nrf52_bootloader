include ../Makefile.boards

CFLAGS += -DBOARD_PCA10059

ASMFLAGS += -DBOARD_PCA10059

ifeq ($(PBRICK_DEBUG), 1)
CFLAGS += -DNRF_LOG_BACKEND_UART_TX_PIN=11
ASMFLAGS += -DNRF_LOG_BACKEND_UART_TX_PIN=11
endif